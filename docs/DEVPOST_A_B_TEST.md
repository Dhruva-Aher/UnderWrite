# Underwrite: Devpost copy (single source)

Use [`DEVPOST.md`](../DEVPOST.md) as the submission text.

**Headline**: DataHub knows where your ML features came from. Underwrite makes CI act on that knowledge.

**Why this framing**: The intellectual contribution is executable metadata — CI that fails on DataHub lineage evidence. Target leakage is the demonstration; metadata-backed deployment authorization is the product. AI remediation is Assist after the block, not equal billing.

## Do not submit

- Defensive lines that argue against "just a linter" before the judge raises it — **show** Observe → Reason → Act → Remember → Assist instead
- Headlines that co-bill "Deterministic Gates + AI Advisors" as two peer features
- Claims about `sqlglot`, AST parsing, or semantic SQL diffs (not in this codebase)
- Column-drop "blast radius" as the primary story (demo policy is **target leakage** via FineGrainedLineage)
- Language that implies the offline fixture path is a live DataHub evaluation
- Stack tours (DFS, FastAPI, React, LangChain) in the video or elevator pitch

## Memory anchor for judges

> DataHub knows where your ML features came from. Underwrite makes CI act on that knowledge.
