# Underwrite — pinned to the environment that passed live GMS verification.
# Direct dependencies only (not a full pip freeze). Re-pin from a clean
# `.venv` after intentionally upgrading any of these packages.

# Web server
fastapi==0.141.1
uvicorn==0.52.1
pydantic-settings==2.15.0
PyYAML==6.0.3

# HTTP clients for DataHub health checks and SDK communication
httpx==0.28.1
requests==2.34.2

# DataHub Python SDK & REST Emitter
acryl-datahub==1.6.0.6

# Test runner used by the documented `python test_full_suite.py` command
pytest==9.1.1
playwright==1.62.0

# LangChain and Agent Context Kit (post-verdict remediation only)
# datahub-agent-context[langchain] pulls in the official LangChain tool bindings
# (build_langchain_tools) from the DataHub Agent Context Kit.
datahub-agent-context[langchain]==1.7.0
langchain==1.3.14
langchain-core==1.5.3
langchain-openai==1.4.2
langchain-anthropic==1.5.4
langchain-google-genai==4.3.2
langgraph==1.2.10
